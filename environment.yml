name: multicampus_project
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.13
  - numpy
  - tqdm
  - pandas
  - pyarrow
  - pip
  - pip:
      - gensim
      - konlpy
      - selenium
      - undetected-chromedriver
      - beautifulsoup4
# 설치 법
# conda env update -f environment.yml
